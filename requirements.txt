# Runtime base v0.1: nessuna dipendenza esterna (solo libreria standard).
#
# Cervello locale (LLM a bordo): il client usa solo urllib (stdlib), quindi
# NON serve alcun pacchetto Python. Il modello gira come servizio SEPARATO,
# non come dipendenza di Jarvis Mini:
#   - server OpenAI-compatible (llama.cpp server / Ollama / TensorRT-LLM)
#   - modello Qwen3-4B-Instruct (GGUF o TensorRT), in ascolto su 127.0.0.1
#   - attivazione lato agente:  JARVIS_AI=local
#
# Voce/biometria REALI (opzionali): installali + imposta le env per attivarli
# al posto dei mock. Senza, l'app resta pienamente funzionante in mock.
#
#   # TTS (voce)         -> JARVIS_TTS=piper JARVIS_PIPER_MODEL=/path/voce.onnx
#   piper-tts
#   # STT (voce->testo)  -> JARVIS_STT=vosk JARVIS_VOSK_MODEL=/path/vosk-it
#   vosk
#   sounddevice
#   # Wake word          -> JARVIS_WAKEWORD=oww JARVIS_OWW_MODEL=/path/hey_gsoi.onnx
#   openwakeword
#   numpy
#   # Speaker ID (sei tu)-> JARVIS_SPEAKER=resemblyzer ; enroll con: jarvis-mini enroll Ana *.wav
#   resemblyzer
#
# Loop vocale nel servizio: JARVIS_VOICE=1
